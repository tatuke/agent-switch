# Claude Code Notes

All cross-agent project rules live in [AGENTS.md](./AGENTS.md). Claude Code reads `AGENTS.md` natively, so that file is the single canonical source — do NOT duplicate its content here.

## Claude-specific

Currently none. When a rule only applies to Claude Code (e.g., permissions behavior, output style, tool-specific overrides), add it below and keep the cross-agent version in `AGENTS.md`.

- (none)
